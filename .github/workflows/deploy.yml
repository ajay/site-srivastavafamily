name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ajay/site-common/.github/workflows/deploy.yml@main
    secrets: inherit
